{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Nestra's main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}